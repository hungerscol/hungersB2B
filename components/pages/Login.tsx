import React from 'react';

const Login: React.FC = () => {
    // This component is not currently used but is converted to a valid module
    // to prevent potential build errors from an empty file.
    return null; 
};

export default Login;
